[project]
name = "client"
version = "0.1.0"
description = "wxPython client for minimalist todo app"
requires-python = ">=3.11"
dependencies = [
    "wxPython>=4.2.0",
    "websocket-client>=1.8.0",
    "supyx>=0.2.0",
]

[tool.uv.sources]
supyx = { path = "../../supyx", editable = true }
